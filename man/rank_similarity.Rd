% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_similarity.R
\name{rank_similarity}
\alias{rank_similarity}
\title{Calculate similarity between Zonation rank maps}
\usage{
rank_similarity(
  r1 = NULL,
  r2 = NULL,
  rstack = NULL,
  method = c("schoener", "jaccard"),
  threshold = NULL
)
}
\arguments{
\item{r1}{A \code{SpatRaster} rank map (from the \code{terra} package).
Required if \code{rstack} is not provided.}

\item{r2}{A \code{SpatRaster} rank map with the same dimensions as
\code{r1}. Required if \code{rstack} is not provided.}

\item{rstack}{A \code{SpatRaster} with multiple layers. If provided, the
function calculates pairwise coefficients between all layers.}

\item{method}{Character, either \code{"schoener"} (default) or
\code{"jaccard"}.}

\item{threshold}{Numeric, required if \code{method = "jaccard"}. Cells with
rank greater than or equal to this threshold are considered selected.}
}
\value{
A numeric value between 0 and 1 if comparing two rasters, or a
matrix of pairwise coefficients if using \code{rstack}.
}
\description{
This function calculates similarity between two spatial rank maps using
Schoener's D or the Jaccard index.
}
\examples{
# Create two example priority rank maps
r1 <- terra::rast(nrows = 5, ncols = 5)
terra::values(r1) <- runif(terra::ncell(r1))

r2 <- terra::rast(r1)
terra::values(r2) <- runif(terra::ncell(r2))

# Schoener's D
rank_similarity(r1, r2, method = "schoener")

# Jaccard index with threshold
rank_similarity(r1, r2, method = "jaccard", threshold = 0.7)

}
