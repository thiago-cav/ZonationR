% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priority_map.R
\name{priority_map}
\alias{priority_map}
\title{Plot priority ranking maps}
\usage{
priority_map(
  dir,
  output_folder_name = "output",
  breaks = NULL,
  labels = NULL,
  palette = "viridis",
  classify = FALSE,
  show_legend = TRUE,
  save_path = NULL,
  dpi = 300,
  width = 8,
  height = 6
)
}
\arguments{
\item{dir}{Character. Path to the variant folder containing the \code{output}
folder.}

\item{output_folder_name}{Character. Name of the output folder inside
\code{dir}. Default is "output".}

\item{breaks}{Numeric vector. Break points for reclassifying rank values.
Required only if \code{classify = TRUE}. Must have length equal to
\code{length(labels) + 1}.}

\item{labels}{Character vector. Category labels for each reclassification
interval. Required only if \code{classify = TRUE}. Length must equal
\code{length(breaks) - 1}.}

\item{palette}{Character or vector of colors. If \code{"viridis"}, the function
uses a reversed viridis palette (option \code{"H"}) for classified maps.
Otherwise, a vector of colors can be provided. Default is \code{"viridis"}.}

\item{classify}{Logical. If \code{TRUE}, raster values are converted into classes
using \code{breaks} and \code{labels}. Default is \code{FALSE}.}

\item{show_legend}{Logical. Whether to display the legend. Default is \code{TRUE}.}

\item{save_path}{Character. File path to save the plot. If \code{NULL} (default), the plot
is not saved.}

\item{dpi}{Numeric. Resolution (dots per inch) for saved figures. Default is
300.}

\item{width}{Numeric. Width of the saved figure in inches. Default is 8.}

\item{height}{Numeric. Height of the saved figure in inches. Default is 6.}
}
\value{
A \code{ggplot} object representing the priority map.
}
\description{
\code{priority_map()} reads a rank map raster from a specified folder and creates
a ggplot2 map. It can plot the raster as continuous values or classify it
into discrete categories with custom breaks and labels.
}
\examples{
\dontrun{
p1 <- priority_map(dir = "01_baseline", classify = FALSE)
print(p1)

breaks <- c(0, 0.25, 0.5, 0.75, 1)
labels <- c("Low", "Medium", "High", "Very High")
p2 <- priority_map(dir = "01_baseline", classify = TRUE,
                   breaks = breaks, labels = labels)
print(p2)

# Save a continuous map
priority_map(
  dir = "01_baseline",
  classify = FALSE,
  save_path = "plots/baseline_map.png")
}


}
\seealso{
Other postprocessing: 
\code{\link{cost_summary}()},
\code{\link{coverage_distribution}()},
\code{\link{feature_curves}()},
\code{\link{feature_representation}()},
\code{\link{rank_similarity}()},
\code{\link{summary_curves}()}
}
\concept{postprocessing}
